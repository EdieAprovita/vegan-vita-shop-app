import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.pexels.com',
        port: '',
        pathname: '/photos/**',
      },
    ],
  },
  transpilePackages: ["@vegan-vita-shop/ui", "@vegan-vita-shop/shared"],
};

export default nextConfig;
